<%= form_with(model: comment) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :up_votes %>
    <%= form.number_field :up_votes %>
  </div>

  <div class="field">
    <%= form.label :down_votes %>
    <%= form.number_field :down_votes %>
  </div>

  <div class="field">
    <%= form.label :send_date %>
    <%= form.datetime_select :send_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
